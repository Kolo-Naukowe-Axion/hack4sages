# Reproduces the environment that generated the POSEIDON side of data/crossgen_biosignatures
# (data/TauREx set/, manifest.json: software_versions.poseidon = "1.3.2").
# Same version used for the K1(c) stage-1 diagnosis, re-run to confirm bit-identical geometry
# to the original 1.4.0 diagnostic run (docs/METHODOLOGICAL_AUDIT.md, K1(c)).
#
# System dependency, not pip-installable: open-mpi (mpi4py links against it).
#   macOS: brew install open-mpi
#   Debian/Ubuntu: apt-get install libopenmpi-dev
#
# setuptools<81 is required because newer setuptools dropped pkg_resources, on which
# pysynphot (imported by POSEIDON.stellar) depends.
setuptools<81
h5py>=3.10,<4
numpy>=1.26,<3
pandas>=2.2,<3
pyarrow>=16
scipy
mpi4py
POSEIDON @ git+https://github.com/MartianColonist/POSEIDON.git@v1.3.2
