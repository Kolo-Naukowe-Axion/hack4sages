"""ExoBiome Streamlit UI layer (Osoba 4).

Modules:
  app                 - Streamlit entry point (run from repo root)
  components          - pure Altair/pandas chart builders (no st.* calls)
  inference_adapter   - thin seam over the frozen checkpoint bridge + FP pipeline
"""
